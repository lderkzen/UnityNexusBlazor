@if (Model is not null)
{
    <Button Class="btn-outline-light">
    @* @onclick="@(() => ModalDialogMessenger.ShowItemDetailsDialog(new ItemInfoModel(Model, _itemLevel, _itemCategory,
    _itemType)))"> *@
    <Row Padding="Padding.Is0">
        <Column Padding="Padding.Is1" Class="item-badge-text text-truncate">
            @Model.Title
        </Column>
    </Row>
</Button>
}
